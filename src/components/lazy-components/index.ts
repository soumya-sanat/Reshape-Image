// import { lazy } from "react";

// export const LazyButton = lazy(() => import("../lazy-components/LazyButton"));
// export const LazyHeader = lazy(() => import("./navigation/header"));

import { LazyButton } from './LazyButton';

export { LazyButton };
